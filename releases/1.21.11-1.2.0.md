- Adult villagers slowly recover health after a post-damage cooldown (default: one Minecraft day)
- Qualifying damage refreshes the cooldown; defense mode pauses regen until stand-down
- Health regen can be toggled and cooldown length adjusted via `healthRegenEnabled` and `healthRegenCooldownTicks` in server config
